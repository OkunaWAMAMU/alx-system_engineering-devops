#!/usr/bin/env bash
# This script displays all active IPv4 IPs on the machine

# Get all active IPv4 IPs using the ip command and filter with grep and awk
ip -4 addr show | grep -oP '(?<=inet\s)\d+(\.\d+){3}'

